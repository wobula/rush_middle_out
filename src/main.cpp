#include "Game.hpp"

int main(void)
{
		World	w(20, 40);
		bool	going;
		// update to take window parameters rather than hardcoded

		while ((going = World.doCycle(void)))
		{
				//add enemies
				// window.clear;
				// window.drawStars;
				// window.drawEntities;
		}
		// game over;
  return (0);
}
